<div class="content-layout">
  <h1 id="page-title" class="content__title">Protected Page</h1>
  <div class="content__body">
    <p id="page-description">
      <span>
        This page retrieves <strong>protected messages</strong>.
      </span>
      <span>
        <strong>Only authenticated users should access this page.</strong>
      </span>
    </p>
    <%= render partial: "layouts/partials/code_snippets", locals: { title: "Protected Messages", messages: @messages.map(&:verbose) } %>
  </div>
</div>